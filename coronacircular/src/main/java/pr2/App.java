package pr2;

/**
 * Hello world!
 *
 * 
 * 
 */
public class App 
{
  /* public double myDonut(){
       
		double x = Math.random()*2-1;
		double y = Math.random()*2-1;
		double distancia = Math.sqrt(x*x+y*y);
            
   }
   int areaCuadrado= 4;

   public double myDonutIterativo(int pasos){
    int aciertos = 0;   
    
       return areaCuadrado*(aciertos/pasos);
   }
   public double myDonutRecursivo(double pasos){
    double aciertos =0;   
    List<Double> array1 = new ArrayList();
       if(array1.isEmpty()){ //caso base
           return areaCuadrado*(aciertos/pasos); // cuando acabe la llmada recursiva, esto es lo que comparara en el test

       }else{
        for(double i= 0; i<pasos; i++){
           array1.add(i);
           if(myDonut().contains(double) i)
            aciertos++;
        }
        
       }
       return myDonutRecursivo(pasos);
   }
   /*public double myDonutLambda(double pasos){
       

        double  resultado = .stream().filter(f-> f.contains())
    
   }*/
}
